<UserControl x:Class="WpfPrism.Views.Dialogs.WaitDialogUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfPrism.Views.Dialogs"
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="350" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Padding="20,10" Text="{Binding ShowTitle}" FontWeight="Bold" FontSize="25"/>

        <StackPanel Grid.Row="1">
            <DockPanel Margin="20" LastChildFill="False">
                <TextBlock Text="状态" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox SelectedIndex="{Binding WaitInfoDTO.Status}">
                    <ComboBoxItem>待办</ComboBoxItem>
                    <ComboBoxItem>已完成</ComboBoxItem>
                </ComboBox>

            </DockPanel>
            <TextBox md:HintAssist.Hint="请输入待办概要" Margin="20,10" DockPanel.Dock="Top" Text="{Binding WaitInfoDTO.Title, UpdateSourceTrigger=LostFocus}"/>
            <TextBox md:HintAssist.Hint="请输入待办内容" Margin="20,10" DockPanel.Dock="Top" MinHeight="100" Text="{Binding WaitInfoDTO.Contents, UpdateSourceTrigger=LostFocus}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="取消" Margin="15,15,7,15" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding CancelCommand}"></Button>
            <Button Content="确认" Margin="8,15,15,15" Command="{Binding SaveCommand}"></Button>
        </StackPanel>

        <!--显示错误提示-->
        <md:Snackbar x:Name="WaitBar" Grid.Row="2" MessageQueue="{md:MessageQueue}"></md:Snackbar>
    </Grid>
</UserControl>
